Java编程练习存档

1.13
程序随机生成100个0-99之间的随机整数存入数组，选取一
种合适的流，将该数组的100个整数写入本地文件。

1.14
使用缓冲流，实现文件的拷贝，文件类型可以选择文本文件或图片文件，请根据不同的文件类型选取不同的流来处理。

1.15
所谓对象序列化就是指将一个保存的对象变成一个二进制的数据流进行传输。如果一个对象需要被序列化，则对象所在的类必须实现Serializable接口。
定义学生类，实现其序列化和反序列化。

1.16
使用随机读写流RandomAccessFile类，实现对长文本文件的随机读写，要求从附件文本文件中，读取出请求头信息的主机地址（Host），和响应头信息的内容类型（Content-Type）

1.17
指定一个本地路径，从该路径及所有子路径获取所有图片类型的文件，得到一个File数组，将其按文件大小排序输出。

1.18
至少用四种方法创建Student类的对象。

1.19
使用反射机制调用类的某个方法。
主要用到下面这个方法：
public Object invoke(Object obj,Object... args)
这个方法对带有指定参数的指定对象调用由此 Method 对象表示的底层方法。

1.20
使用Java反射获取类名，方法名，属性及构造方法。

1.21
使用泛型和反射封装JDBC数据库基础操作类public class BaseDao<T>

1.22
用继承Thread类，实现Runnable接口，实现Callable接口几种方法实现Java线程，实现每隔1秒递增输出一个从1开始的自然数。

1.23
至少用3种以上方法实现一个定时器，模拟闹钟提醒功能。

1.24～1.26
春节休息

1.27
使用多线程模拟多窗口售票程序，注意数据的同步。

1.28
学习线程池的基础使用方法。创建线程池，结合IO下载远程图片。

1.29
结合GUI和多线程，实现如下功能:
1.同步显示系统时间
2.播放音乐
3.图片轮播
以上每个功能分别对应一个线程，不要在主线程实现。

1.30
定时任务(一)
了解TimerTask实现定时或周期性任务的方式，并知道其缺陷。用它来实现每隔几秒时间，生成一个二维码图片的功能。

1.31
定时任务(二）
学习使用ScheduledThreadPoolExecutor来实现定时周期性的任务，了解其比起Timer的优势。
用它来实现每天早上八点输出当日信息热榜。控制台先摸拟打印，进阶是爬取网络数据。

2.1
JDBC之BaseDao增删改查完整封装。

2.2
MyBatis之BaseMapper封装。

2.3
Socket和ServerSocket起步，了解这两个类以及使用方法。

2.4
客户端和服务端之间通信，传送文本数据。

2.5
客户端和服务端之间通信，传送非文本数据。

2.6
服务端向各客户端群发数据。

2.7
服务端向指定客户端发送数据。

2.8
单例模式

2.9
工厂模式

2.10
代理模式

2.11
装饰器模式

2.12
装饰器模式

2.13
适配器模式

2.14
建造者模式

2.15
掌握time包日期时间相关API，以及和Date、TimeStamp的转化，建议写个必要的工具类。

2.16
了解Lambda表达式，函数式编程

2.17
了解NIO，stream等JDK8后的特性

2.18
了解目前Java开发的一些技术栈，如JWT、微服务、容器，持续集成等。

未完待续
1.maven全面学习
2. Linux 常用命令
3.注解
4.JVM，类加载机制，GC